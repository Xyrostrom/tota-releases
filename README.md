# tmod desktop installers

Auto-update + manual download artifacts for the tmod desktop app.

Source code lives in a private repo. This repo only hosts release
artifacts so electron-updater + the website `/api/download` endpoint
can fetch them anonymously.

Latest installer: https://github.com/Xyrostrom/tota-releases/releases/latest